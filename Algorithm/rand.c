#include<stdio.h>
#include<stdlib.h>
#include<time.h>

main()
{
	int no;

	srand(time(0));
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);
	no = rand();
	printf("発生した乱数は%dです\n", no);

}